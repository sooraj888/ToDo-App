import { AppBar, Toolbar } from "@mui/material";
import React from "react";

const AppHedder = () => {
  return (
    <AppBar position="static">
      <Toolbar>
        <h2>Todo List</h2>
      </Toolbar>
    </AppBar>
  );
};

export default AppHedder;
